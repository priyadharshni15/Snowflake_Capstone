version: 2

models:
  - name: stg_account
    description: "Staged account data with cleaned column names"
    columns:
      - name: account_id
        tests:
          - not_null
          - unique
